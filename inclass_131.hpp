/*
This is in class example 1
*/

#include <iostream>
#include <fstream>
#include <string>

namespace mySpace{
    namespace yourSpace{
        class myClass{
            public:

                 //construct myClass with an intial name, and number of students
                myClass(std::sting name, int init_num_students);
                myClass)std::string name);

                int get_num_students();
                std::string get_name();
                void set_num_students(int num);


            private:
                int num_students;
                std::string className;


}
    }
}
